// prisma-client-rust 代码生成器 CLI 入口。
// 通过 `cargo prisma <command>`（见 ../.cargo/config.toml 的别名）调用，
// 例如 `cargo prisma generate` 依据 ../prisma/schema.prisma 生成 ../src/prisma.rs。
fn main() {
    prisma_client_rust_cli::run();
}
